server:
  port: 8070
spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP

  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: '*'




